<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Console appender with detailed formatting -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- File appender for persistent logs -->
    <appender name="FILE" class="ch.qos.logback.core.FileAppender">
        <file>sleepy-recorder.log</file>
        <append>true</append>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{50} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Root logger configuration -->
    <root level="INFO">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE" />
    </root>

    <!-- Set specific package logging levels for debugging -->
    <logger name="com.sleepy.recorder" level="DEBUG" />

    <!-- Reduce noise from external libraries -->
    <logger name="org.concentus" level="WARN" />
    <logger name="javafx" level="WARN" />
</configuration>
